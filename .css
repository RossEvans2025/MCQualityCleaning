:root {
  --black: #050505;
  --charcoal: #111111;
  --white: #ffffff;
  --cream: #fff8ef;
  --pink: #f0569a;
  --rose: #ff8fbd;
  --blush: #ffe1ef;
  --gold: #d9a85b;
  --soft-gold: #f6d89a;
  --gray: #666666;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: Arial, sans-serif;
  color: var(--black);
  background: var(--cream);
  line-height: 1.6;
}

.container {
  width: min(1120px, 92%);
  margin: auto;
}

.site-header {
  background: rgba(0, 0, 0, 0.96);
  position: sticky;
  top: 0;
  z-index: 1000;
  border-bottom: 2px solid var(--pink);
}

.nav-wrap {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 0;
}

.logo-wrap img {
  width: 145px;
  display: block;
}

.nav {
  display: flex;
  gap: 28px;
}

.nav a {
  color: var(--white);
  text-decoration: none;
  font-weight: 700;
  letter-spacing: 0.4px;
}

.nav a:hover {
  color: var(--rose);
}

.menu-toggle {
  display: none;
  background: none;
  border: none;
  color: var(--white);
  font-size: 30px;
  cursor: pointer;
}

.hero {
  position: relative;
  overflow: hidden;
  color: var(--white);
  padding: 120px 0;
  background:
    radial-gradient(circle at top left, rgba(240,86,154,.35), transparent 32%),
    radial-gradient(circle at bottom right, rgba(217,168,91,.25), transparent 34%),
    linear-gradient(135deg, #000000 0%, #14070f 45%, #050505 100%);
}

.hero::after {
  content: "";
  position: absolute;
  inset: 18px;
  border: 1px solid rgba(246,216,154,.35);
  pointer-events: none;
}

.hero-grid {
  position: relative;
  z-index: 2;
  display: grid;
  grid-template-columns: 1.25fr .75fr;
  gap: 42px;
  align-items: center;
}

.eyebrow {
  color: var(--gold);
  font-weight: 900;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  margin-bottom: 12px;
}

.hero h1 {
  font-size: clamp(2.6rem, 5vw, 5rem);
  line-height: 1.02;
  margin-bottom: 22px;
}

.hero p {
  max-width: 680px;
  font-size: 1.16rem;
}

.hero-buttons {
  margin-top: 30px;
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
}

.btn {
  display: inline-block;
  padding: 14px 26px;
  border-radius: 999px;
  text-decoration: none;
  font-weight: 900;
  border: none;
  cursor: pointer;
}

.primary {
  background: linear-gradient(135deg, var(--pink), var(--rose));
  color: var(--white);
  box-shadow: 0 12px 28px rgba(240,86,154,.35);
}

.secondary {
  color: var(--soft-gold);
  border: 2px solid var(--gold);
}

.feature-card {
  background: rgba(255,255,255,.96);
  color: var(--black);
  padding: 34px;
  border-radius: 24px;
  border: 2px solid var(--gold);
  box-shadow: 0 20px 50px rgba(0,0,0,.45);
}

.feature-card h3 {
  color: var(--pink);
  font-size: 1.7rem;
  margin-bottom: 10px;
}

.sparkle {
  position: absolute;
  color: var(--soft-gold);
  font-size: 54px;
  opacity: .75;
}

.sparkle-one {
  right: 12%;
  top: 18%;
}

.sparkle-two {
  left: 7%;
  bottom: 18%;
}

.section {
  padding: 82px 0;
}

.section-title {
  text-align: center;
  max-width: 760px;
  margin: 0 auto 44px;
}

.section-title h2,
.about h2 {
  font-size: clamp(2rem, 4vw, 3rem);
  line-height: 1.15;
}

.services {
  background: var(--cream);
}

.cards {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 22px;
}

.card {
  background: var(--white);
  padding: 30px;
  border-radius: 22px;
  border: 1px solid rgba(217,168,91,.35);
  box-shadow: 0 14px 35px rgba(0,0,0,.08);
  border-top: 6px solid var(--pink);
}

.card span {
  display: inline-block;
  color: var(--gold);
  font-size: 32px;
  margin-bottom: 12px;
}

.card h3 {
  margin-bottom: 10px;
  color: var(--charcoal);
}

.about {
  background:
    linear-gradient(rgba(0,0,0,.88), rgba(0,0,0,.88)),
    linear-gradient(135deg, #000, #28111d);
  color: var(--white);
}

.about-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 46px;
  align-items: center;
}

.lux-box {
  background: rgba(255,255,255,.07);
  padding: 34px;
  border-radius: 24px;
  border: 1px solid rgba(246,216,154,.45);
}

.lux-box h3 {
  color: var(--gold);
  margin-bottom: 16px;
  font-size: 1.6rem;
}

.lux-box ul {
  list-style: none;
}

.lux-box li {
  padding: 12px 0;
  border-bottom: 1px solid rgba(255,255,255,.14);
}

.lux-box li::before {
  content: "✦ ";
  color: var(--pink);
}

.quote-section {
  background: linear-gradient(180deg, var(--cream), var(--blush));
}

.quote-form {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 18px;
  max-width: 900px;
  margin: auto;
  background: var(--white);
  padding: 32px;
  border-radius: 24px;
  box-shadow: 0 18px 45px rgba(0,0,0,.12);
  border: 1px solid rgba(217,168,91,.35);
}

.quote-form input,
.quote-form select,
.quote-form textarea {
  padding: 15px;
  border: 1px solid #ddd;
  border-radius: 14px;
  font-size: 1rem;
}

.quote-form textarea {
  grid-column: 1 / -1;
  min-height: 140px;
}

.quote-form button {
  grid-column: 1 / -1;
  justify-self: center;
}

.footer {
  background: var(--black);
  color: var(--white);
  padding: 50px 0;
  border-top: 3px solid var(--pink);
}

.footer-grid {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 34px;
  align-items: center;
}

.footer img {
  width: 180px;
}

.footer h3 {
  color: var(--rose);
  margin-bottom: 10px;
}

@media (max-width: 850px) {
  .menu-toggle {
    display: block;
  }

  .nav {
    display: none;
    position: absolute;
    top: 86px;
    right: 4%;
    width: 230px;
    padding: 20px;
    border-radius: 18px;
    background: var(--black);
    flex-direction: column;
    box-shadow: 0 12px 30px rgba(0,0,0,.4);
  }

  .nav.active {
    display: flex;
  }

  .hero-grid,
  .about-grid,
  .footer-grid,
  .cards,
  .quote-form {
    grid-template-columns: 1fr;
  }

  .hero {
    padding: 90px 0;
  }

  .hero::after {
    inset: 10px;
  }
}
